---
description: 战斗实体管线·引擎无关内核（spec + EngineAdapter + 打包）；自研/多引擎项目必读
alwaysApply: false
---

# 战斗实体 Core（可移植）

## 与望月 Unity 专线的关系

| 层级 | 路径 | 打包给新引擎 |
| --- | --- | --- |
| **Core 内核** | `tools/fight_entity_core/`、`run_fight_entity_pipeline.py` | **必须** |
| **Core Agent** | 本规则 + `wy-fight-entity-core-doc` | **必须** |
| **望月 Profile** | `wy-fight-entity-orchestrator-doc`、`link-to-fight-entity-partner-spec`、Mo1F 等 rules | **仅 Unity 望月工程** |

自研引擎：只打包 Core + Core Agent；**不要**默认打包全套 `fight-entity-*` 望月 rules（除非仍用 `unity_wangyue` adapter）。

## 契约

1. 真源（Excel / DataTable / JSON）→ **`spec.json`**（`fight_entity_core/spec.schema.json`）
2. **`pipeline_profile`** 选择 `EngineAdapter`（`stub_engine` | `unity_wangyue` | 自研注册名）
3. CLI：`python tools/run_fight_entity_pipeline.py --entity-id <id> --profile <profile>`
4. 收口以 `[pipeline-ledger] converged=true` 为准

## 新引擎接入

实现 `fight_entity_core/adapter.py` 的六个钩子并 `@register_adapter`；Intake 扩展 `fight_entity_core/intake.py`。

## 打包

manifest 组件：`fight_entity_core` + `agent_core`（见 `fight_entity_pipeline_bundle_manifest.json`）。
