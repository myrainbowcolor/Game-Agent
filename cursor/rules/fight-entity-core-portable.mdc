---
description: Game-Agent 战斗实体内核（spec + EngineAdapter）；跨项目通用，不含宿主实现
alwaysApply: false
---

# Fight Entity Core（框架层）

## 分层

| 层级 | 职责 | 所在仓库 |
| --- | --- | --- |
| **框架** | `fight_entity_core`、`run_fight_entity_pipeline.py`、本规则 | [Game-Agent](https://github.com/myrainbowcolor/Game-Agent) |
| **宿主** | `fight_entity_host_<project>/`、表映射、引擎写盘 | 各游戏项目仓 |
| **项目 Agent** | `<project>-agent-profile`、orchestrator Skill | 各游戏项目仓 |

## 契约

1. 真源（Excel / DataTable / JSON）→ **`spec.json`**（`spec.schema.json`）
2. **`pipeline_profile`** = 已注册的 `EngineAdapter.profile_id`
3. CLI 须显式 `--spec` 或 `--intake`；宿主 adapter 用 `--host-package <pkg>` 或 `FIGHT_ENTITY_HOST_PACKAGES` 注册
4. 收口以 `[pipeline-ledger] converged=true` 为准

## 框架 CLI

```bash
python <framework>/python/run_fight_entity_pipeline.py \
  --entity-id <id> --profile stub_engine --intake <spec.json>
```

## 宿主接入

1. 在**项目仓**实现 `fight_entity_host_<project>/` 并 `@register_adapter`
2. 项目包装脚本负责 `--host-package` 与项目默认路径（**不要**写进框架 CLI）
3. 扩展 intake 映射或项目侧 `*_intake.py`

## 禁止

- 在框架仓 hardcode 任一游戏项目包名、表路径、实体号段
- 在框架 Skill 中写宿主 orchestrator 细则
